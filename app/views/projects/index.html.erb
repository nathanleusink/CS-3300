<p id="notice"><%= notice %></p>

<h1>Projects</h1>

<table>

  </head>
    <main role="main">

      <!-- Main jumbotron for a primary marketing message or call to action -->
      <div class="jumbotron">
        <div class="container">
          <h1 class="display-3">Welcome!</h1>
          <p>This website is designed to showcase and provide ease of access for coding projects. Anyone is allowed to view the projects. However, if you want to edit the projects, you will need to authenticate yourself and justify your need to do so.</p>
        </div>
      </div>

      <!---- Cards ----->
      <div class="container-fluid padding">
      <div class="row padding">
        <% @projects.each do |project| %>
          <tr> 
            <div class="col-md-4">
            <h2><%=project.title%></h2>
            <p><%=project.description%></p>
            <td><%= link_to 'Show', project %></td>
            <td><%= link_to 'Edit', edit_project_path(project) %></td>
            <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </div>
        <hr>
      </div> <!-- /container -->

    </main>
</table>

<br>

<button type="button" class="btn btn-primary btn-lg btn-block" href="projects/new">New Project</button>
<%= link_to 'New Project', new_project_path %>